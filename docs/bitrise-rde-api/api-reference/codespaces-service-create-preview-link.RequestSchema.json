{"title":"Body","body":{"content":{"application/json":{"schema":{"description":"CreatePreviewLinkRequest mints a stateless, Ed25519-signed preview link.\nNothing is stored server-side — the response token is the only artifact.\nEvery session a link spawns is owned by the workspace, whoever minted it: a\nuser minter (PAT) is recorded on those sessions as attribution only, a\nWorkspace API Token minter leaves no user on them at all.","properties":{"artifact":{"description":"The app build to install on each open.","properties":{"appName":{"description":"App display name, echoed to the viewer page.","type":"string"},"buildNumber":{"description":"CI build number, echoed to the viewer page.","type":"string"},"commitSha":{"description":"Built commit SHA, echoed to the viewer page.","type":"string"},"url":{"description":"Signed, expiring download URL for the app artifact. The session VM\ndownloads it directly — the backend never touches the bytes. It expires\nindependently of the link.","type":"string"}},"type":"object"},"deviceSpec":{"description":"The device to request on each open.","properties":{"coldBoot":{"description":"Android only: boot the emulator cold (-no-snapshot-load) and never save a\nquickboot snapshot, so every open exercises a full first boot.","type":"boolean"},"cores":{"description":"Android only: emulator CPU core count. 0 = sized to the host (cores/2,\nclamped). Same mint bound + on-VM clamp as ram_mb.","format":"int64","type":"integer"},"deviceModel":{"description":"Device profile hint (for example, \"pixel_7\"). Empty = platform default.","type":"string"},"osVersion":{"description":"iOS only: the simulator runtime to boot, as an iOS version such as \"18.2\"\n(or a simctl runtime id). Anything else, and any value on Android, is\nrejected. Empty = the newest installed runtime. Reads back as the version.","type":"string"},"platform":{"description":"Device platform: \"android\" (emulator, Linux stacks) or \"ios\" (simulator,\nmacOS stacks).","type":"string"},"ramMb":{"description":"Android only: emulator RAM in MB. 0 = sized to the host (RAM/4, clamped).\nBounded at mint against the machine type's specs; clamped again on the VM\nwith a degradation notice if the host cannot honor it.","format":"int64","type":"integer"},"systemImage":{"description":"Android only: sdkmanager system image package (for example,\n\"system-images;android-34;google_apis;x86_64\"). Empty = platform\ndefault. Images not pre-installed on the stack fall back to the best\ninstalled one with a viewer-visible degradation notice — they are never\ndownloaded at boot.","type":"string"}},"type":"object"},"machineType":{"description":"Machine type the link's sessions run on. Empty = the platform default.","type":"string"},"sessionAutoTerminateMinutes":{"description":"Idle auto-terminate window (minutes) for sessions this link spawns: a\nsession ends this long after the last viewer disconnects. 0 uses the\ndefault of 60. When set, it must be at least 10 minutes (a smaller window\nwould reap sessions mid-boot, before the first viewer stream can extend\nthe deadline) and at most 480 — the 8-hour session max lifetime always\nbounds a session's total runtime regardless of this window, so\nauto-terminate can be tuned but never turned off.","format":"int32","type":"integer"},"stackId":{"description":"Stack the link's sessions run on. Empty = the platform default. There is\ndeliberately no cluster field — the cluster is derived from the stack +\nmachine type combination, same as session creation.","type":"string"},"ttlSeconds":{"description":"Link lifetime in seconds. 0 uses the default of 24 hours; capped at 72\nhours. A short lifetime is the primary abuse control — there is no\nrevocation list, so anyone holding the URL can open a device until the\nlink expires.","format":"int64","type":"string"},"warmPoolId":{"description":"Serve the link's opens from a warm pool (see WarmPool): each open claims\none of the pool's warm sessions when one is available and creates a\nsession from the pool's configuration otherwise, so the click-to-app time\nis the app download, not a VM boot. The pool must be WORKSPACE-owned (a\nuser pool's sessions carry personal credentials that must never reach an\nanonymous viewer), visible to the minter, and its configuration must boot\na device. The pool fixes the machine and the device: stack_id and\nmachine_type must be empty, and device_spec must be empty, name only the\npool's platform (what the Device Preview Step sends, detected from the\napp), or equal the pool's device. Sessions the link spawns are owned by the workspace; the\nminter is attribution. Deleting the pool invalidates every link minted\nwith it: later opens fail like an expired link. A pool that still exists\nbut no longer boots the link's device degrades the open to the ordinary\ncold path instead (ResolvePreviewLinkResponse.fallback_reason).","type":"string"}},"type":"object"}}},"required":true}}