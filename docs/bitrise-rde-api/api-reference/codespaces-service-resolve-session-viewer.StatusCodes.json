{"responses":{"200":{"description":"A successful response.","content":{"application/json":{"schema":{"description":"ResolveSessionViewerResponse carries the viewer bootstrap for a session's\ndevice view — the same message a preview link's resolve returns, with\nattach_only set — so the viewer page runs one code path for both.","properties":{"view":{"description":"The viewer bootstrap: stream grant, gateway routing, device and install state.","properties":{"appName":{"description":"App metadata from the link, for the viewer page header.","type":"string"},"attachOnly":{"description":"True when the response opens an existing session's device view\n(ResolveSessionViewer): the page attaches to that session and never\ncreates one, so the delete/reopen affordances do not apply and a gone\nsession is simply gone.","type":"boolean"},"attached":{"description":"True when the caller was attached to their existing live session for\nthis link instead of a new one being created.","type":"boolean"},"buildNumber":{"description":"CI build number from the link.","type":"string"},"commitSha":{"description":"Built commit SHA from the link.","type":"string"},"deviceNotes":{"description":"Human-readable device detail reported by the VM itself: why the device\nFAILED, or how it degraded from the requested configuration while still\nworking (for example, \"requested system image X not installed; using Y\").\nEmpty when the device came up exactly as requested — and always empty for\nsessions whose VM scripts predate the signal.","type":"string"},"deviceSpec":{"description":"The device spec the link requested.","properties":{"coldBoot":{"description":"Android only: boot the emulator cold (-no-snapshot-load) and never save a\nquickboot snapshot, so every open exercises a full first boot.","type":"boolean"},"cores":{"description":"Android only: emulator CPU core count. 0 = sized to the host (cores/2,\nclamped). Same mint bound + on-VM clamp as ram_mb.","format":"int64","type":"integer"},"deviceModel":{"description":"Device profile hint (for example, \"pixel_7\"). Empty = platform default.","type":"string"},"osVersion":{"description":"iOS only: the simulator runtime to boot, as an iOS version such as \"18.2\"\n(or a simctl runtime id). Anything else, and any value on Android, is\nrejected. Empty = the newest installed runtime. Reads back as the version.","type":"string"},"platform":{"description":"Device platform: \"android\" (emulator, Linux stacks) or \"ios\" (simulator,\nmacOS stacks).","type":"string"},"ramMb":{"description":"Android only: emulator RAM in MB. 0 = sized to the host (RAM/4, clamped).\nBounded at mint against the machine type's specs; clamped again on the VM\nwith a degradation notice if the host cannot honor it.","format":"int64","type":"integer"},"systemImage":{"description":"Android only: sdkmanager system image package (for example,\n\"system-images;android-34;google_apis;x86_64\"). Empty = platform\ndefault. Images not pre-installed on the stack fall back to the best\ninstalled one with a viewer-visible degradation notice — they are never\ndownloaded at boot.","type":"string"}},"type":"object"},"deviceState":{"description":"Device readiness verdict, asserted by the VM itself. Visible to anyone\nholding the link (resolve is unauthenticated), which is fine: it carries\nnothing beyond boot progress.","default":"PREVIEW_DEVICE_STATE_UNSPECIFIED","enum":["PREVIEW_DEVICE_STATE_UNSPECIFIED","PREVIEW_DEVICE_STATE_BOOTING","PREVIEW_DEVICE_STATE_READY","PREVIEW_DEVICE_STATE_FAILED"],"type":"string"},"fallbackReason":{"description":"Human-readable reason when the link's requested stack/machine type could\nnot be provisioned and this open fell back to the deployment defaults\n(for example, the machine type was retired after the link was minted).\nEmpty when the session runs exactly what was requested. Only set on the\nresolve that created the session — attaches to an existing session\nreturn it empty.","type":"string"},"installReason":{"description":"Human-readable reason why the app install FAILED (the install script's\ntyped error, e.g. \"simctl install failed: … IncompatibleArchitecture …\"),\nsanitized and bounded server-side. Set only while install_status is\nFAILED; empty otherwise, and always empty for sessions written by\nbackends that predate the field.","type":"string"},"installStatus":{"description":"State of the app install on the device.","default":"PREVIEW_INSTALL_STATUS_UNSPECIFIED","enum":["PREVIEW_INSTALL_STATUS_UNSPECIFIED","PREVIEW_INSTALL_STATUS_PENDING","PREVIEW_INSTALL_STATUS_RUNNING","PREVIEW_INSTALL_STATUS_OK","PREVIEW_INSTALL_STATUS_FAILED"],"type":"string"},"machineType":{"description":"Machine type this open's session actually runs on. Empty on sessions\ncreated before this field existed.","type":"string"},"sessionId":{"description":"ID of the device session serving this open.","type":"string"},"sessionStatus":{"description":"Current session status (VM state).","default":"SESSION_STATUS_UNSPECIFIED","enum":["SESSION_STATUS_UNSPECIFIED","SESSION_STATUS_PENDING","SESSION_STATUS_STARTING","SESSION_STATUS_RUNNING","SESSION_STATUS_TERMINATING","SESSION_STATUS_FAILED","SESSION_STATUS_TERMINATED","SESSION_STATUS_DRAINING","SESSION_STATUS_DRAINED","SESSION_STATUS_UNKNOWN"],"type":"string"},"stackId":{"description":"Stack this open's session actually runs on (the link's requested stack,\nthe deployment default, or the fallback). Empty on sessions created\nbefore this field existed.","type":"string"},"streamBaseUrl":{"description":"Absolute base URL (scheme + host, no path, no trailing slash) of the\nDC-local stream gateway serving this session's media endpoints\n(RDE-480). When set, the viewer builds every stream/asset/control URL\nagainst it (base + stream_path etc.) instead of the API host; empty\nmeans the legacy same-host media path. Chosen server-side per session\ncluster from deployment config, gated by the enable-rde-stream-gateway\nworkspace flag and the gateway's live health. The rollout and fallback\nlevers, with honest latencies for NEW and RECONNECTING connections: a\nflag flip lands within one resolve poll (≤3s pre-stream, ≤60s\nstreaming); the map is deploy config, so changing it takes a pod roll\nplus one poll; a gateway OUTAGE additionally waits on health detection\nfirst — the API's probe (15s cadence + up to a 5s timeout), stacked on\nthe gateway's own cached /readyz self-check (10s cadence + 5s timeout,\ndemoting a ready gateway only after 3 consecutive failures, ~35s) when\nthe failure is gateway-side, e.g. a stale token or a severed\ngateway-to-control-plane path. Established\nstreams are never cut by any lever — they migrate on their next\nreconnect or at the 30-minute connection cap; the emergency drain of a\nlive gateway is stopping its process.","type":"string"},"streamGrant":{"description":"Short-lived (max 120s) Ed25519-signed stream grant. The viewer presents\nit on the stream WebSocket; it re-calls resolve to refresh before expiry.","type":"string"},"streamGrantExpiresAt":{"description":"When the stream grant expires.","format":"date-time","type":"string"},"streamPath":{"description":"Path of the stream WebSocket on this API host\n(for example, \"/ws/preview/{session_id}/stream\").","type":"string"}},"type":"object"}},"type":"object"}}}}}}