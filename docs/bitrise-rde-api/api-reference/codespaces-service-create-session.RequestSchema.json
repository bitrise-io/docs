{"title":"Body","body":{"content":{"application/json":{"schema":{"description":"CreateSessionRequest is the request for creating a new session.","properties":{"aiPrompt":{"description":"Optional AI prompt to pass to Claude Code when the session starts.","type":"string"},"artifact":{"description":"Optional app build to install on the device once it is ready — the same\nmessage a preview link carries. Requires device_spec. The install runs in\nthe background; Session.device.install_status reports its progress. The\nURL is stored on the session (never returned) so the install can run\nwithout a viewer polling.","properties":{"appName":{"description":"App display name, echoed to the viewer page.","type":"string"},"buildNumber":{"description":"CI build number, echoed to the viewer page.","type":"string"},"commitSha":{"description":"Built commit SHA, echoed to the viewer page.","type":"string"},"url":{"description":"Signed, expiring download URL for the app artifact. The session VM\ndownloads it directly — the backend never touches the bytes. It expires\nindependently of the link.","type":"string"}},"type":"object"},"autoTerminateMinutes":{"description":"Minutes before auto-termination. Defaults to 7200 (5 days).\nSet to 0 to disable auto-termination (\"Never\").","format":"int32","type":"integer"},"cluster":{"description":"Target cluster name. Required when the template's image + machine type are available\nin multiple clusters. If only one cluster matches, this can be omitted.","type":"string"},"description":{"description":"Description of the session.","type":"string"},"deviceSpec":{"description":"Virtual device to boot with this session: an Android emulator (Linux\nstacks) or an iOS simulator (macOS stacks) — the same message a device\npreview link carries, so links and sessions describe devices identically.\nWhen set:\n  - the session boots the device and streams it exactly like a preview\n    link's session (see Session.device for readiness);\n  - with a template that declares a device (Template.device_spec), the\n    request is an override: WITH a platform it is the complete device to\n    boot (empty fields = platform defaults, the template's are ignored);\n    WITHOUT a platform it tweaks the template's device field by field\n    (empty fields inherit); no device_spec at all boots the template's\n    device as declared; no_device skips it;\n  - stack_id and machine_type may be omitted on a template-less session:\n    the deployment's per-platform device defaults apply, and the cluster\n    is picked deterministically instead of erroring on ambiguity;\n  - a supplied stack/machine type must fit the platform (OS family, spec\n    floor) or the request is rejected — no silent fallback.\nAbsent = no device. auto_terminate_minutes keeps its ordinary default\neither way. Gated per platform by the enable-rde-android-emulator /\nenable-rde-ios-simulator workspace flags.","properties":{"coldBoot":{"description":"Android only: boot the emulator cold (-no-snapshot-load) and never save a\nquickboot snapshot, so every open exercises a full first boot.","type":"boolean"},"cores":{"description":"Android only: emulator CPU core count. 0 = sized to the host (cores/2,\nclamped). Same mint bound + on-VM clamp as ram_mb.","format":"int64","type":"integer"},"deviceModel":{"description":"Device profile hint (for example, \"pixel_7\"). Empty = platform default.","type":"string"},"osVersion":{"description":"iOS only: the simulator runtime to boot, as an iOS version such as \"18.2\"\n(or a simctl runtime id). Anything else, and any value on Android, is\nrejected. Empty = the newest installed runtime. Reads back as the version.","type":"string"},"platform":{"description":"Device platform: \"android\" (emulator, Linux stacks) or \"ios\" (simulator,\nmacOS stacks).","type":"string"},"ramMb":{"description":"Android only: emulator RAM in MB. 0 = sized to the host (RAM/4, clamped).\nBounded at mint against the machine type's specs; clamped again on the VM\nwith a degradation notice if the host cannot honor it.","format":"int64","type":"integer"},"systemImage":{"description":"Android only: sdkmanager system image package (for example,\n\"system-images;android-34;google_apis;x86_64\"). Empty = platform\ndefault. Images not pre-installed on the stack fall back to the best\ninstalled one with a viewer-visible degradation notice — they are never\ndownloaded at boot.","type":"string"}},"type":"object"},"enabledFeatureFlagNames":{"description":"Names of feature flags to enable.","items":{"type":"string"},"type":"array"},"image":{"description":"Machine image name. When a template is given, optionally overrides the\ntemplate's image for this session.\n\nSuperseded by stack_id (prefer it). Accepted for now and resolved to a stack id on the fly.","type":"string"},"labels":{"additionalProperties":{"type":"string"},"description":"Arbitrary key/value metadata to attach to the session (for example, team,\nbranch, ticket id). At most 32 entries (also enforced on the post-merge\nresult of updates); keys are 1-63 chars of [a-zA-Z0-9._/-] starting and\nending alphanumeric; values are 1-255 bytes of [a-zA-Z0-9._/:+-] (fits\ntimestamps, branches, paths). The \"bitrise.io/\" key prefix is reserved\nfor system-owned labels and rejected on user writes.","type":"object"},"machineType":{"description":"Machine type name. Required when template_id is empty. When a template is\ngiven, optionally overrides the template's machine type for this session.","type":"string"},"mapSavedToSessionInputs":{"description":"When true, the backend fetches the current user's saved inputs and\nauto-fills any template session inputs (matched by key) that the request\ndid not already supply, before required-input validation runs. The keys\nthat were auto-filled are reported in CreateSessionResponse.auto_mapped_inputs.","type":"boolean"},"name":{"description":"Human-readable name of the session.","type":"string"},"noDevice":{"description":"Create the session WITHOUT the template's declared device\n(Template.device_spec). Only meaningful with such a template; cannot be\ncombined with device_spec.","type":"boolean"},"sessionInputs":{"description":"Session input values (key/value pairs or saved input references).","items":{"type":"object","description":"SessionInputValue provides a value for a session input when creating a session.\nEither provide a direct value or reference a saved input by ID (for secrets).","properties":{"isSecret":{"description":"Whether the value is secret (encrypt at rest). Ignored if saved_input_id is set.","type":"boolean"},"key":{"description":"Key name matching a session input on the template.","type":"string"},"savedInputId":{"description":"Optional: ID of a Saved Input to use. Backend fetches and decrypts the value server-side.\nThis avoids exposing secret values to the frontend.","type":"string"},"value":{"description":"Direct value (used when user typed in a value). Ignored if saved_input_id is set.","type":"string"}}},"type":"array"},"stackId":{"description":"Stack id (for example, \"osx-xcode-16.0.x-edge\"). Required when template_id is empty\n(unless the deprecated image is supplied). When a template is given,\noptionally overrides the template's stack for this session. Preferred over image.","type":"string"},"templateId":{"description":"ID of the template to use. Optional: omit to create a session without a\ntemplate. When empty, image and machine_type are required and no\nwarmup/startup scripts run.","type":"string"}},"type":"object"}}},"required":true}}