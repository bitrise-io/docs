---
title: "Steps reference"
description: "A Step encapsulates a build task: the code to perform that task, the inputs and parameters you can define for the task, and the outputs the task generates."
sidebar_position: 2
slug: /bitrise-ci/references/steps-reference
---

Steps reference

A Step encapsulates a build task: the code to perform that task, the inputs and parameters you can define for the task, and the outputs the task generates.
