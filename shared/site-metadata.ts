// Plain constants only — no Node APIs. Imported both by docusaurus.config.ts
// (webpack build) and functions/og.js (Cloudflare Pages Function bundle),
// which have no filesystem or Docusaurus runtime available to it.
export const DEFAULT_DESCRIPTION =
  'Find product documentation, code samples, API & CLI references, and more.';
