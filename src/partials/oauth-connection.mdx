import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import GlossTerm from '@site/src/components/GlossTerm';

## OAuth connection

An OAuth connection allows a third-party application to access a user's data on a service without sharing their login credentials. On Bitrise, an OAuth connection means connecting your Bitrise account to your Git provider account.

Bitrise supports an OAuth connection with three Git providers: GitHub, GitLab, and Bitbucket. With an OAuth connection, Bitrise can:

- Retrieve and display a list of the available repositories when [adding a new project](/en/bitrise-build-cache/getting-started-with-the-build-cache/getting-started-with-the-build-cache#adding-a-new-connection-to-the-build-cache).
- [Automatically register webhooks](/en/bitrise-platform/integrations/webhooks/adding-incoming-webhooks#registering-an-incoming-webhook-automatically). Webhooks allow setting up <GlossTerm baseform="Trigger">build triggers</GlossTerm> and enable the use of [Git Insights](/en/insights/git-insights).
- [Automatically register SSH keys](/en/bitrise-platform/repository-access/configuring-ssh-keys).

To set up an OAuth connection, check out: [Repository access with OAuth](/en/bitrise-platform/repository-access/repository-access-with-oauth).
