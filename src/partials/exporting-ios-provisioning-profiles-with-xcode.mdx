import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import GlossTerm from '@site/src/components/GlossTerm';

## Exporting iOS provisioning profiles with Xcode

1. Start Xcode.
1. Select `Xcode > Preferences` from the navigation bar.
1. At the top of the window select `Accounts`.
1. Select your Apple ID and your team from the right side bar, then click on `View Details...`.
1. A dialog will appear where you will see your code signing identities and the provisioning profiles.
1. Locate the profile that you are looking for under Provisioning Profiles.
1. If you don’t have it installed on the system, click on the `Download` button next to it.
1. Choose `Show in finder` from the pop-up menu, that will show you the installed provisioning profile for you.
