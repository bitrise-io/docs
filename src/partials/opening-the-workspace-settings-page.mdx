import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import GlossTerm from '@site/src/components/GlossTerm';

1. Log in to Bitrise and hover over the left navigation bar.
1. Make sure you have the right <GlossTerm baseform="workspace">workspace</GlossTerm> selected in the **Workspace** menu.
1. Select **Settings**.
