import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import GlossTerm from '@site/src/components/GlossTerm';
import NT from '@site/src/components/NT';

:::important[No <NT>Step</NT> input modifications needed]

With this method, all <NT>Step inputs</NT> related to authenticating with an <NT>Apple</NT> API key are automatically populated once the connection is set up. You don't have to manually modify those <NT>Step</NT> input fields at all.

:::
