import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import GlossTerm from '@site/src/components/GlossTerm';

## Unselecting notification preferences

By default Bitrise sends newsletters and platform updates to the email address you provide under your **Profile**. You can unselect this setting with 3 simple steps:

1. Click **Profile settings** under your avatar.
1. Click **Notification** on the left menu bar. You can see that Newsletters and Platform updates are both enabled by default.
1. If you wish to rather not get any or one of the two, notifications, untick the relevant checkbox/es.
