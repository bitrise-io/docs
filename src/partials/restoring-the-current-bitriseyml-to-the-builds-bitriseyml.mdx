import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import GlossTerm from '@site/src/components/GlossTerm';

## Restoring the configuration YAML

You can always change the current configuration YAML to the configuration of one of your app's previous builds. This overrides the current configuration. To do so:

1. Open Bitrise CI, select a project and then select one of the builds of the project.
1. Select **Configuration YAML** on the top right.
1. In the dropdown menu, select **Show configuration YAML**.
1. In the dialog, click the **Restore build's configuration YAML**.
1. Click **OK** in the dialog.
