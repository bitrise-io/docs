import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import GlossTerm from '@site/src/components/GlossTerm';
import NT from '@site/src/components/NT';

:::caution[SAML SSO restrictions]

SAML SSO is only available for a <NT><GlossTerm baseform="workspace">Workspace</GlossTerm></NT> with [our paid plans](https://bitrise.io/pricing).

Since the SAML SSO feature is tied to the above plans, if you decide to downgrade to a free plan, you will lose this feature. All <NT>Workspace</NT> members will receive an email about the downgrade, and you’ll have two weeks to re-upgrade if you wish to use SAML SSO in your <NT>Workspace</NT> again.

:::
