import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import GlossTerm from '@site/src/components/GlossTerm';

## Annotating a build

To use annotations:

1. Add a <GlossTerm baseform="Step">Step</GlossTerm> capable of running custom commands.

   For example, **Script** or **fastlane**.
1. Add a command to install the Bitrise Annotations plugin:

   ```bash
   bitrise plugin install https://github.com/bitrise-io/bitrise-plugins-annotations.git
   ```
1. Use the `annotate` command of the plugin to create your annotations.

   The command works in the following format: `bitrise :annotations annotate [markdown] [flags]`. This is a basic Markdown annotation in the default style:

   ```bash
   bitrise :annotations annotate "**My Annotation**"
   ```
